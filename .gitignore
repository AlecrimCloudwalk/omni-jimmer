# Dependencies
node_modules/

# Environment files (keep examples tracked)
.env
.env.*
!.env.example
!env.example

# OS / Editor
.DS_Store
*.log

# Supabase local development & config with secrets
.supabase/
supabase/.env
supabase/config.toml
supabase-config.js
# docs/supabase-config.js (temporarily allowed for OAuth testing)

# Firebase config with real keys (if any)
firebase-config.js

# OAuth credentials and sensitive configs
github-oauth-config.js
oauth-credentials.js

.vercel
